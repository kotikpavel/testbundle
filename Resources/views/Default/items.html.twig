{% extends 'KotikTestBundle::layout.html.twig' %}
{% block body %}
    <h1>Render Items</h1>

    <div class="items">
        {% for item in items %}
          <div class="item">
              {{ item }}
          </div>
        {% endfor %}
    </div>

{% endblock %}